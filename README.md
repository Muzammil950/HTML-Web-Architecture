# HTML-Web-Architecture
HTML (HyperText Markup Language) is the foundation of the web. It defines the structure of web pages, while other technologies like CSS and JavaScript handle style and behavior.
